package io.javaclasses.language;
/*
    In this package implemented service that compile and interpret a result of statements.
    Statements may contain initialization and methods.
    Methods are print(a),delete(b), cleanScreen(), where:
            a is some previously initialized variable or expression,
            b is a previously initialized variable or expression.
 */